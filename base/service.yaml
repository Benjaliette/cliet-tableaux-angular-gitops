apiVersion: v1
kind: Service
metadata:
  name: cliet-tableaux-angular
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: cliet-tableaux-angular
